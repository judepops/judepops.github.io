# Ignore the contents of the _site directory and other cache directories
_site/
.sass-cache/

# Ignore the directory for local files during development
local/

# Ignore the Gemfile that is generated
Gemfile.lock

# Ingore files created by npm
node_modules
package-lock.json
# Working folder: scripts, notes and drafts used to build the site
# but which are not part of the published site. Never committed.
_cowork/
